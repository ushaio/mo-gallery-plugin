# Security Policy

请不要在公开 Issue 中提交可利用的插件漏洞、签名材料或凭据。通过 GitHub Security
Advisory 私下报告，并包含受影响插件 ID、版本、平台、复现步骤和影响范围。

签名私钥必须保存在发布环境的受限 Secret 中，不得提交到此仓库。怀疑密钥泄露时，
应立即停止使用该密钥、撤下受影响的市场版本，并在 Desktop 发布中轮换信任根。

市场索引、Release 资产 SHA-256 和包内 Ed25519 签名属于不同校验层；任何一层通过都
不能作为跳过其他层的理由。
